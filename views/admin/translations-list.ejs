<div class="mb-8">
  <h1 class="text-3xl font-bold text-gray-900">Translations</h1>
  <p class="text-gray-500 mt-2">Manage translations for all languages</p>
</div>

<div class="grid md:grid-cols-2 lg:grid-cols-4 gap-4">
  <% languages.forEach(function(lang) { %>
    <a href="/admin/translations/<%= lang.code %>"
       class="bg-white rounded-xl shadow-sm p-6 hover:shadow-md transition-shadow">
      <div class="flex items-center mb-4">
        <span class="w-12 h-12 bg-primary-100 rounded-lg flex items-center justify-center font-bold text-primary-900 text-lg">
          <%= lang.code.toUpperCase() %>
        </span>
      </div>
      <h3 class="font-bold text-gray-900"><%= lang.name %></h3>
      <p class="text-sm text-gray-500 mt-1">Edit translations</p>
    </a>
  <% }); %>
</div>

<div class="mt-8 bg-blue-50 rounded-xl p-6">
  <h3 class="font-bold text-blue-900 mb-2">Translation Tips</h3>
  <ul class="text-blue-800 text-sm space-y-2">
    <li>• English (EN) is the base language - translate all other languages from English</li>
    <li>• Keep the same structure and keys across all languages</li>
    <li>• After saving, changes are immediately visible on the website</li>
    <li>• Use the preview feature to check your translations before publishing</li>
  </ul>
</div>
