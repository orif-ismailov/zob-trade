<%- include('../layouts/main', { body: `

<!-- Page Header -->
<section class="bg-primary-900 text-white py-16">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <h1 class="text-4xl font-bold mb-4">${__('about.title')}</h1>
    <p class="text-xl text-gray-300 max-w-3xl">${__('about.subtitle')}</p>
  </div>
</section>

<!-- Company Overview -->
<section class="py-16 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid lg:grid-cols-2 gap-12">
      <div>
        <h2 class="text-3xl font-bold text-primary-900 mb-6">${__('about.who_we_are.title')}</h2>
        <div class="prose prose-lg text-gray-600">
          <p class="mb-4">${__('about.who_we_are.p1')}</p>
          <p class="mb-4">${__('about.who_we_are.p2')}</p>
          <p>${__('about.who_we_are.p3')}</p>
        </div>
      </div>

      <div class="bg-gray-50 rounded-2xl p-8">
        <h3 class="text-xl font-bold text-primary-900 mb-6">${__('about.company_info.title')}</h3>
        <dl class="space-y-4">
          <div>
            <dt class="text-sm text-gray-500">${__('about.company_info.legal_name')}</dt>
            <dd class="text-lg font-medium text-primary-900">ZOB TRADE LLC</dd>
          </div>
          <div>
            <dt class="text-sm text-gray-500">${__('about.company_info.registration')}</dt>
            <dd class="text-lg font-medium text-primary-900">Georgia</dd>
          </div>
          <div>
            <dt class="text-sm text-gray-500">${__('about.company_info.experience')}</dt>
            <dd class="text-lg font-medium text-primary-900">25 ${__('about.company_info.years')}</dd>
          </div>
          <div>
            <dt class="text-sm text-gray-500">${__('about.company_info.regions')}</dt>
            <dd class="text-lg font-medium text-primary-900">${__('about.company_info.regions_value')}</dd>
          </div>
        </dl>
      </div>
    </div>
  </div>
</section>

<!-- Our Ports -->
<section class="py-16 bg-gray-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <h2 class="text-3xl font-bold text-primary-900 mb-12 text-center">${__('about.ports.title')}</h2>

    <div class="grid md:grid-cols-3 gap-8">
      <div class="bg-white rounded-xl p-8 text-center">
        <div class="w-20 h-20 bg-primary-100 rounded-full flex items-center justify-center mx-auto mb-6">
          <svg class="w-10 h-10 text-primary-900" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
          </svg>
        </div>
        <h3 class="text-xl font-bold text-primary-900 mb-2">Poti Port</h3>
        <p class="text-gray-600">${__('about.ports.poti_desc')}</p>
      </div>

      <div class="bg-white rounded-xl p-8 text-center">
        <div class="w-20 h-20 bg-primary-100 rounded-full flex items-center justify-center mx-auto mb-6">
          <svg class="w-10 h-10 text-primary-900" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
          </svg>
        </div>
        <h3 class="text-xl font-bold text-primary-900 mb-2">Kulevi Terminal</h3>
        <p class="text-gray-600">${__('about.ports.kulevi_desc')}</p>
      </div>

      <div class="bg-white rounded-xl p-8 text-center">
        <div class="w-20 h-20 bg-primary-100 rounded-full flex items-center justify-center mx-auto mb-6">
          <svg class="w-10 h-10 text-primary-900" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
          </svg>
        </div>
        <h3 class="text-xl font-bold text-primary-900 mb-2">Batumi Port</h3>
        <p class="text-gray-600">${__('about.ports.batumi_desc')}</p>
      </div>
    </div>
  </div>
</section>

<!-- Trading Model -->
<section class="py-16 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <h2 class="text-3xl font-bold text-primary-900 mb-12 text-center">${__('about.model.title')}</h2>

    <div class="max-w-3xl mx-auto">
      <div class="relative">
        <div class="absolute left-4 top-0 bottom-0 w-0.5 bg-primary-200"></div>

        <div class="relative pl-12 pb-8">
          <div class="absolute left-0 w-8 h-8 bg-primary-900 rounded-full flex items-center justify-center text-white font-bold">1</div>
          <h3 class="text-lg font-bold text-primary-900 mb-2">${__('about.model.step1_title')}</h3>
          <p class="text-gray-600">${__('about.model.step1_desc')}</p>
        </div>

        <div class="relative pl-12 pb-8">
          <div class="absolute left-0 w-8 h-8 bg-primary-900 rounded-full flex items-center justify-center text-white font-bold">2</div>
          <h3 class="text-lg font-bold text-primary-900 mb-2">${__('about.model.step2_title')}</h3>
          <p class="text-gray-600">${__('about.model.step2_desc')}</p>
        </div>

        <div class="relative pl-12 pb-8">
          <div class="absolute left-0 w-8 h-8 bg-primary-900 rounded-full flex items-center justify-center text-white font-bold">3</div>
          <h3 class="text-lg font-bold text-primary-900 mb-2">${__('about.model.step3_title')}</h3>
          <p class="text-gray-600">${__('about.model.step3_desc')}</p>
        </div>

        <div class="relative pl-12">
          <div class="absolute left-0 w-8 h-8 bg-accent-600 rounded-full flex items-center justify-center text-white font-bold">4</div>
          <h3 class="text-lg font-bold text-primary-900 mb-2">${__('about.model.step4_title')}</h3>
          <p class="text-gray-600">${__('about.model.step4_desc')}</p>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- CTA -->
<section class="py-16 bg-primary-900 text-white">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <h2 class="text-3xl font-bold mb-4">${__('about.cta.title')}</h2>
    <p class="text-xl text-gray-300 mb-8">${__('about.cta.subtitle')}</p>
    <a href="${langUrl('/contact')}" class="inline-flex items-center bg-accent-600 text-white px-8 py-4 rounded-lg text-lg font-semibold hover:bg-accent-700 transition-colors">
      ${__('about.cta.button')}
    </a>
  </div>
</section>

` }) %>
