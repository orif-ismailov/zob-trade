<%- include('../layouts/main', { body: `

<!-- Page Header -->
<section class="bg-primary-900 text-white py-16">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <h1 class="text-4xl font-bold mb-4">${__('logistics.title')}</h1>
    <p class="text-xl text-gray-300 max-w-3xl">${__('logistics.subtitle')}</p>
  </div>
</section>

<!-- Ports Section -->
<section class="py-16 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <h2 class="text-3xl font-bold text-primary-900 mb-12 text-center">${__('logistics.ports.title')}</h2>

    <div class="grid md:grid-cols-3 gap-8">
      <div class="border border-gray-200 rounded-xl overflow-hidden hover:shadow-lg transition-shadow">
        <div class="h-64 overflow-hidden">
          <img src="/poti_port.jpg" alt="Poti Port" class="w-full h-64 object-cover">
        </div>
        <div class="p-6">
          <h3 class="text-xl font-bold text-primary-900 mb-2">Poti Port</h3>
          <p class="text-gray-600 mb-4">${__('logistics.ports.poti_desc')}</p>
          <ul class="text-sm text-gray-500 space-y-1">
            <li>• ${__('logistics.ports.poti_feature1')}</li>
            <li>• ${__('logistics.ports.poti_feature2')}</li>
          </ul>
        </div>
      </div>

      <div class="border border-gray-200 rounded-xl overflow-hidden hover:shadow-lg transition-shadow">
        <div class="h-64 overflow-hidden">
          <img src="/kulevi_terminal.webp" alt="Kulevi Terminal" class="w-full h-64 object-cover">
        </div>
        <div class="p-6">
          <h3 class="text-xl font-bold text-primary-900 mb-2">Kulevi Terminal</h3>
          <p class="text-gray-600 mb-4">${__('logistics.ports.kulevi_desc')}</p>
          <ul class="text-sm text-gray-500 space-y-1">
            <li>• ${__('logistics.ports.kulevi_feature1')}</li>
            <li>• ${__('logistics.ports.kulevi_feature2')}</li>
          </ul>
        </div>
      </div>

      <div class="border border-gray-200 rounded-xl overflow-hidden hover:shadow-lg transition-shadow">
        <div class="h-64 overflow-hidden">
          <img src="/batumi_port.jpg" alt="Batumi Port" class="w-full h-64 object-cover">
        </div>
        <div class="p-6">
          <h3 class="text-xl font-bold text-primary-900 mb-2">Batumi Port</h3>
          <p class="text-gray-600 mb-4">${__('logistics.ports.batumi_desc')}</p>
          <ul class="text-sm text-gray-500 space-y-1">
            <li>• ${__('logistics.ports.batumi_feature1')}</li>
            <li>• ${__('logistics.ports.batumi_feature2')}</li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Rail Routes -->
<section class="py-16 bg-gray-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <h2 class="text-3xl font-bold text-primary-900 mb-12 text-center">${__('logistics.routes.title')}</h2>

    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <div class="space-y-6">
        <div class="bg-white rounded-xl p-6 border-l-4 border-primary-900">
          <h3 class="font-bold text-primary-900 mb-2">${__('logistics.routes.uzbekistan')}</h3>
          <p class="text-gray-600">${__('logistics.routes.uzbekistan_desc')}</p>
        </div>

        <div class="bg-white rounded-xl p-6 border-l-4 border-primary-900">
          <h3 class="font-bold text-primary-900 mb-2">${__('logistics.routes.kazakhstan')}</h3>
          <p class="text-gray-600">${__('logistics.routes.kazakhstan_desc')}</p>
        </div>

        <div class="bg-white rounded-xl p-6 border-l-4 border-accent-600">
          <h3 class="font-bold text-primary-900 mb-2">${__('logistics.routes.afghanistan')}</h3>
          <p class="text-gray-600">${__('logistics.routes.afghanistan_desc')}</p>
        </div>

        <div class="bg-white rounded-xl p-6 border-l-4 border-primary-900">
          <h3 class="font-bold text-primary-900 mb-2">${__('logistics.routes.tajikistan')}</h3>
          <p class="text-gray-600">${__('logistics.routes.tajikistan_desc')}</p>
        </div>
      </div>

      <div class="bg-gray-100 rounded-2xl p-4 overflow-hidden">
        <img src="/oil-tanker-logistics.jpg" alt="Rail Routes from Georgia to Central Asia" class="w-full h-auto rounded-lg">
      </div>
    </div>
  </div>
</section>

<!-- Incoterms -->
<section class="py-16 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <h2 class="text-3xl font-bold text-primary-900 mb-12 text-center">${__('logistics.incoterms.title')}</h2>

    <div class="grid md:grid-cols-4 gap-6">
      <div class="bg-primary-50 rounded-xl p-6 text-center">
        <div class="text-3xl font-bold text-primary-900 mb-2">FOB</div>
        <p class="text-sm text-gray-600">${__('logistics.incoterms.fob')}</p>
      </div>

      <div class="bg-primary-50 rounded-xl p-6 text-center">
        <div class="text-3xl font-bold text-primary-900 mb-2">CIF</div>
        <p class="text-sm text-gray-600">${__('logistics.incoterms.cif')}</p>
      </div>

      <div class="bg-primary-50 rounded-xl p-6 text-center">
        <div class="text-3xl font-bold text-primary-900 mb-2">DAP</div>
        <p class="text-sm text-gray-600">${__('logistics.incoterms.dap')}</p>
      </div>

      <div class="bg-accent-50 rounded-xl p-6 text-center">
        <div class="text-3xl font-bold text-accent-600 mb-2">DDP</div>
        <p class="text-sm text-gray-600">${__('logistics.incoterms.ddp')}</p>
      </div>
    </div>
  </div>
</section>

<!-- CTA -->
<section class="py-16 bg-primary-900 text-white">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <h2 class="text-3xl font-bold mb-4">${__('logistics.cta.title')}</h2>
    <p class="text-xl text-gray-300 mb-8">${__('logistics.cta.subtitle')}</p>
    <a href="${langUrl('/contact')}" class="inline-flex items-center bg-accent-600 text-white px-8 py-4 rounded-lg text-lg font-semibold hover:bg-accent-700 transition-colors">
      ${__('logistics.cta.button')}
    </a>
  </div>
</section>

` }) %>
